# nordvpn_config_swapper
For a router loaded with OpenWrt firmware and configured for OpenVPN, this Bash script simply uploads a VPN config file and edits the OpenVPN init.d config to point to that file.
